---
import FocusCard from "@/components/home/FocusCard.astro"
import { focusGroups } from "@/content/home.ts"
---

<div class="bg-[#ebebeb] p-4 sm:p-6 lg:p-8">
  <section
    class="container grid grid-cols-1 gap-6 max-lg:place-items-center lg:grid-cols-3"
  >
    {focusGroups.map((group) => <FocusCard {...group} />)}
  </section>
</div>
