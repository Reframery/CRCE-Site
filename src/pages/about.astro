---
import Layout from "@/layouts/Layout.astro"
import { Image } from "astro:assets"
import * as content from "@/content/about.ts"
import banner from "@/images/about/banner.jpg"
import image from "@/images/about/middle.jpg"
import { cn } from "@/lib/utils"

const title = "About"
---

<Layout {title} description={content.about.main}>
  <Image
    src={banner}
    alt="DeGroote School of Business, Building"
    class="border-gold mx-auto max-h-80 border-b-[6px] object-cover object-[50%,66%]"
    loading="eager"
  />
  <section class="container p-8 md:py-12">
    <h1
      class="border-maroon mx-auto mb-4 w-fit border-b-4 pb-0.5 text-2xl font-semibold"
    >
      {content.about.title}
    </h1>
    <p class="mx-auto max-w-[680px] text-center text-base md:text-lg">
      {content.about.main}
    </p>
  </section>
  <section class="bg-gallery grid overflow-hidden md:mb-12 md:grid-cols-2">
    <Image
      src={image}
      alt="Group of people having a meeting"
      class="self-stretch object-cover shadow-mcmaster shadow-nevada/40 lg:max-h-[450px]"
    />
    <div class="space-y-6 self-center p-8 md:space-y-12">
      {
        content.missionVision.map((item) => (
          <div>
            <h2 class="border-maroon mb-2 w-max border-b-4 pb-0.5 text-xl font-semibold">
              {item.title}
            </h2>
            <p>{item.main}</p>
          </div>
        ))
      }
    </div>
  </section>
  <div class="bg-nevada p-8 md:py-12">
    <section
      class="divide-modal-background container grid divide-y overflow-hidden rounded-sm bg-white shadow-md max-lg:max-w-2xl md:divide-x lg:grid-cols-2"
    >
      {
        content.cards.map((item, i) => (
          <div
            class={cn(
              "group space-y-4 p-6 text-center transition-all duration-[250ms] hover:shadow-mcmaster md:p-8",
              i == 1 && "md:!border-y-0",
              i == 2 && "md:!border-x-0"
            )}
          >
            <div class="border-gold group-hover:bg-gold mx-auto size-40 rounded-full border-4 p-8 shadow-nevada transition-all duration-200 group-hover:shadow-mcmaster">
              <Image src={item.image} alt={item.title} />
            </div>
            <h3 class="text-2xl font-semibold">{item.title}</h3>
            <hr class="bg-maroon mx-auto h-[5px] w-10" />
            <p class="text-nevada">{item.main}</p>
          </div>
        ))
      }
    </section>
  </div>
</Layout>
